environment:
  nodejs_version: "0.10"

install:
  - ps: $fileContent = "-----BEGIN RSA PRIVATE KEY-----`n"
  - ps: $fileContent += $env:priv_key.Replace(' ', "`n")
  - ps: $fileContent += "`n-----END RSA PRIVATE KEY-----`n"
  - ps: Set-Content c:\users\appveyor\.ssh\id_rsa $fileContent
  - npm install > nul
  - npm install -g coffee-script > nul

  - coffee "src\send-message.coffee" "About to trigger azure deployment"
  #- ssh git@github.com
  - git clone git@github.com:tm-build/tm-qa-azure.git
  

  #- curl --data "[tm-ci-workflow] about to trigger azure deployment" "https://securityinnovation.slack.com/services/hooks/slackbot?token=%SLACK_TOKEN%&channel=tm-deploy"

  #- node_modules/.bin/coffee test.coffee

  #- cd tm-qa-azure
  #- call git commit --allow-empty -m "empty commit from Appveyor to trigger azure deployment"
  #- git push azure master
  #- cd ..
  #- echo "...done"

build: off
